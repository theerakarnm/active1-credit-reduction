# Booking System MVP: Credit Deduction with Race Condition Protection

โปรเจกต์นี้เป็น Proof of Concept (PoC) จำลองระบบการจอง (Booking System) ที่มีความละเอียดอ่อนเรื่อง การตัดเงิน (Credit Deduction) และ ทรัพยากรที่มีจำกัด (Limited Slots) โดยเน้นการแก้ปัญหา Race Condition ที่มักเกิดขึ้นเมื่อมีผู้ใช้งานหลายคนกดจองพร้อมกัน

## 🎯 ปัญหาที่ต้องการแก้ (The Problem)
ในระบบจองทั่วไป หากเขียน Code แบบ "อ่านค่า -> ตรวจสอบ -> บันทึก" (Check-then-Act) โดยไม่มีการจัดการ Transaction ที่ดี มักจะเจอ 2 ปัญหาใหญ่:
1. Overbooking/Double Booking: Slot เดียวกันถูกจองโดยลูกค้า 2 คน เพราะระบบอ่านค่าว่า "ว่าง" พร้อมกัน
2. Ghost Deduction/Negative Balance: เงินถูกตัดไปแล้วแต่จองไม่ได้ หรือเงินติดลบเพราะตัดซ้อนกันหลาย Thread

## 💡 แนวคิดทางเทคนิค (The Solution: "Safety First" Flow)
ระบบนี้ใช้แนวคิด ACID Database Transaction มาจำลองในฝั่ง Frontend (Mock Backend) โดยมีขั้นตอนการทำงาน 4 Step หลัก:
1. 🔒 Lock (Mutex): * ทันทีที่มี Request เข้ามา ระบบจะทำการ Lock Resource (ทั้ง User Wallet และ Slot ID)
   - Request อื่นที่เข้ามาทีหลังจะต้อง เข้าคิว (Queue) รอจนกว่า Transaction แรกจะจบ
2. 👀 Check (Fresh Read):
   - อ่านค่าล่าสุดจาก Database หลังจากที่ได้ Lock แล้ว เท่านั้น
   - ตรวจสอบว่า Slot ยังว่างอยู่จริงไหม? และ เงินพอไหม?
3. 💸 Execute:
   - หากผ่านการตรวจสอบ จะทำการตัดเครดิตและเปลี่ยนสถานะ Slot
4. 💾 Commit / Rollback:
   - Success: ยืนยันการบันทึกข้อมูล (Commit)
   - Error: หากเกิดข้อผิดพลาดขั้นตอนใดขั้นตอนหนึ่ง ระบบจะคืนค่าทั้งหมดกลับสู่สภาพเดิม (Rollback) ทันที

## 🛠 ฟีเจอร์จำลอง (Simulation Features)
1. Mock Database with Mutex
เราเขียน Class Mutex ขึ้นมาเองเพื่อจำลอง Row-Level Locking ของ Database จริง เพื่อให้เห็นภาพการทำงานแบบ Serializability (ทำทีละรายการตามลำดับ)
2. Network Latency Simulation
สามารถปรับเลื่อนแถบ Environment Settings เพื่อจำลองความหน่วงของอินเทอร์เน็ต (Delay) ได้ ทำให้เห็นช่วงเวลาที่เสี่ยงต่อการเกิด Race Condition ชัดเจนขึ้น
3. Chaos Mode (Race Condition Attack)
ปุ่ม สายฟ้า (⚡) ใช้จำลองสถานการณ์ที่มี User กดแย่งกันจอง Slot เดียวกันพร้อมกัน 3 คน (ยิง 3 Requests ในเสี้ยววินาที) เพื่อพิสูจน์ว่าระบบ Locking ทำงานได้จริง

## 🧪 วิธีการทดสอบ (How to Test)

### Scenario A: การจองปกติ (Happy Path)
1. กดปุ่ม "จองปกติ" ที่ Slot ใดก็ได้
2. สังเกต Log ฝั่งขวา จะเห็น Flow: INIT -> LOCK -> CHECK -> EXECUTE -> COMMIT
3. เครดิตลดลง และสถานะเปลี่ยนเป็น Booked

### Scenario B: การแย่งกันจอง (Race Condition Test)
1. กดปุ่ม Reset มุมขวาบน
2. ปรับ Network Latency ไปที่ประมาณ 1500ms - 2000ms
3. กดปุ่ม สายฟ้า (⚡) ที่ Slot ใด Slot หนึ่ง
4. ผลลัพธ์ที่ควรจะเป็น:
   - Log จะแสดง Transaction 3 รายการ (INIT) เข้ามาพร้อมกัน
   - จะมีแค่ 1 รายการ ที่ทำรายการสำเร็จ (COMMIT)
   - อีก 2 รายการ จะรอจนกว่ารายการแรกจบ แล้วจะฟ้อง Error ว่า ❌ Slot ไม่ว่าง (ROLLBACK) โดยที่เงินจะไม่ถูกตัดซ้ำซ้อน

## 💻 Tech Stack
- React (TypeScript): UI และ Logic
- Tailwind CSS: Styling
- Lucide React: Iconography
- Custom Mutex Logic: Asynchronous locking mechanism using Promises
- Created for demonstrating High-Concurrency Booking Logic.# active1-credit-reduction
